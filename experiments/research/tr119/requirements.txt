# TR119 experiment dependencies.
# Install via: pip install -r scripts/tr119/requirements.txt

# Core ML stack (reuses TR118's deps)
torch>=2.5.1
transformers>=4.48.0
numpy>=2.1.0
pandas>=2.2.2
scipy>=1.13.1
matplotlib>=3.10.0
pyyaml>=6.0.2

# Resource monitoring (for power/energy telemetry)
psutil>=5.9.0
pynvml>=11.5.0; platform_system!="Darwin"
pywin32>=306; platform_system=="Windows"

# Optional: cloud pricing APIs (if we want to fetch real-time pricing)
# requests>=2.31.0  # For cloud API calls
# boto3>=1.34.0  # AWS pricing API
# google-cloud-billing>=1.11.0  # GCP pricing API
# azure-mgmt-costmanagement>=4.0.0  # Azure pricing API

