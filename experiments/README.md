# Research Workspace

Shared raw data lives in `data/` and is consumed by per-report directories (`TR111`, `TR112`, `TR114`). Each TR folder holds its own scripts and artifacts while PublishReady contains the final markdown reports.
