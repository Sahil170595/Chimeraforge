# Dependencies are managed in pyproject.toml
#
# pip install chimeraforge              # base (planner only)
# pip install "chimeraforge[bench]"     # benchmarking
# pip install "chimeraforge[eval]"      # quality evaluation
# pip install "chimeraforge[dev]"       # development (pytest, ruff)
# pip install "chimeraforge[all]"       # everything
#
# For editable install:
# pip install -e ".[dev]"
#
# For legacy experiment reproduction (TR108-TR123), see requirements-experiments.txt
